# Tauri / Node build output — never committed; CI installs + builds fresh.
node_modules/
dist/
src-tauri/target/
src-tauri/gen/
# Staged in CI (bundled runtime source + uv binary) — never committed.
src-tauri/resources/
src-tauri/binaries/
