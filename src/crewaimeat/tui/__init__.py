"""crewaimeat fleet TUI — a lazydocker-style monitor/manager for the fleet.

Phase 1 ships `fleet_state` (the pure, testable data layer). The Textual UI (app/widgets) and the
action wrappers land in later phases. See docs/internal/tui-plan.md.
"""
